apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  # Secret은 kubectl로 직접 생성 (보안상 Git에 커밋하지 않음)
  # - secret.yaml
