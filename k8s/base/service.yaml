apiVersion: v1
kind: Service
metadata:
  name: hotdeal-bot-service
  labels:
    app: hotdeal-bot
spec:
  type: ClusterIP
  # Discord 봇은 HTTP 서버가 아니므로 포트는 필요 없음
  # 하지만 Service는 생성하여 Pod 선택자로 사용
  selector:
    app: hotdeal-bot
